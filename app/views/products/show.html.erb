<h3>
  <a name="top">&nbsp;</a>
  <%= @product.name %>
</h3>

<dl class="dl-horizontal">
  <%= show_field(
    t('areas.index.title'), raw(@product.areas.map{|a| link_to a.name, area_path(a)}.join(', '))
  ) %>
</dl>

<%= markdown @product.text %>