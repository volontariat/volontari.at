<%= form_for(@area) do |f| %>
  <%= render partial: 'shared/form/error_messages', locals: { resource: @area } %>
  
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
